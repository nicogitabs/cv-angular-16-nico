import { UrlToLabelPipe } from './url-to-label.pipe';

describe('UrlToLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new UrlToLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
